# 敏感信息与漏洞反馈

LiveTrans 将配置和日志保存在 `%APPDATA%\livetrans`。API 密钥目前以明文保存在本机 JSON 中；请使用受限额度的密钥，并保护系统账户和该目录。

本地识别使用 faster-whisper；启用云端识别会把音频片段发送到配置的识别服务。翻译接口会收到识别文本，本地 Ollama 与远端服务的区别由配置地址决定。

请勿在公开 Issue、PR、Actions 日志或截图中发布密钥、带认证信息的 URL、原始音频或私密字幕。提交日志前先检查并删去相关内容。

如果发现真实凭据泄漏，请先撤销或轮换凭据；删除工作区文件不能清除 Git 历史中的副本。项目提供静态检查并在 CI 中使用 Gitleaks，但扫描通过不代表不存在所有形式的敏感信息。

报告漏洞时，优先使用仓库 Security 页面中的私密漏洞报告入口（如已启用）；否则先发不含漏洞细节和敏感数据的 Issue，请维护者提供私密联系渠道。

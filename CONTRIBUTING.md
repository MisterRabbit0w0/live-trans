# 参与开发

请先描述要解决的问题、复现步骤和 Windows / Python / Qt 版本。音频捕获与原生窗口功能目前依赖 Windows。

开发安装和离线检查命令见 [README](README.md#开发与验收)。自动回归使用假捕获和假引擎；真实设备、模型及服务测试需要自行配置，不应在 CI 中调用真实 API。

提交前运行 Ruff、Python 编译检查和 `scripts/check_public_tree.py --staged`。将界面、管线和发布配置按功能分开提交，并在 PR 中写明实际执行的检查和未验证的部分。不要在截图、日志或配置示例中附上真实密钥或识别文本。

窗口合成、焦点、缩放和 DPI 改动需要 Windows 桌面验收，不能仅凭离屏 QML 检查通过就声明已验证。
